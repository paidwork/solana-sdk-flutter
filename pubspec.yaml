name: worken_sdk
description: "SDK library providing access to make easy and secure transactions with Worken."
version: 2.0.0
homepage: https://www.paidwork.com/

environment:
  sdk: ">=3.2.6 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dartz: ^0.10.1
  solana: ^0.30.4
  equatable: ^2.0.5
  get_it: ^7.6.7
  injectable: ^2.3.2
  bip39: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  injectable_generator: ^2.4.1
  build_runner: ^2.3.3
